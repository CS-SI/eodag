outputs_prefix: # The top level directory where all the products will be downloaded
extract: # true or false (if true, extraction is done right after the download of a product)
airbus-ds:
    credentials:
        apikey:
