instance-1:
  priority: 10000
  search:
    plugin: ArlasSearch
    api_endpoint: 'http://www.domain.com'
    products:
      PROD-TYPE:
        product_type: L1C
        collection: Sentinel-2
  download:
    plugin: HTTPDownload
    on_site: false
    base_uri: 'https://www.domain.com/api/download'
  crunch:
    plugin: RemoveDoubles
  auth:
    plugin: Header
    auth_uri: 'https://www.domain.com/api/authidentity'
    token_type: json
