usgs:
  priority: 5
  api:
    extract: False
    credentials:
      username: usr
      password: pwd

aws_eos:
  search:
    product_location_scheme: 'file'
  auth:
    credentials:
      apikey: api-key
      aws_access_key_id: access-key-id
      aws_secret_access_key: secret-access-key

peps:
  download:
    outputs_prefix: /data

theia:
  download:
    outputs_prefix:
